Comparação com GitHub Pages: no endereço https://serftecnologia.github.io/polaris-teste/ `window.POLARIS_CONFIG` aponta para o mesmo projeto e `window.supabase === true`; o cliente `window.sb` também existe. No domínio https://polaris.serftecnologia.com.br/ a configuração é idêntica, mas `window.supabase === false` no bootstrap. A falha é específica da ordem/execução do SDK externo no Cloudflare, provavelmente causada por defer/Rocket Loader/cache, e não por senha, usuário ou projeto Supabase diferente.
